#include <stdio.h>

/**
 * main - prints base 10 numbers
 * Return: return 0 upon successful run
 */

int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
		printf("%d", num);
	printf("\n");

	return (0);
}
