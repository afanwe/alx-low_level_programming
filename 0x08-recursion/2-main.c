#include "main.h"
#include <stdio.h>

/**
 * main - checks and excutes code
 * Return: 0 upon successful execution
 */

int main(void)
{
	int n;

	n = _strlen_recursion("Corbin Coleman");
	printf("%d\n", n);
	return (0);
}
