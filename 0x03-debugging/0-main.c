#include "main.h"

/**
 * main - tests the function that prints if a integer is pos,neg or 0
 * Return: returns 0 upon successful
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
