#include "main.h"

/**
 * main - checks code for errors
 * Return: 0 if ther is no errors
 */

int main(void)
{
	print_triangle(2);
	print_triangle(10);
	print_triangle(1);
	print_triangle(0);

	return (0);
}
