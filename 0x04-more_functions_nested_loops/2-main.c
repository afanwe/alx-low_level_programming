#include "main.h"
#include <stdio.h>

/**
 * main - will display the multiple of the integers
 * Return: 0 if runs with no error
 */

int main(void)
{
	printf("%d\n", mul(98, 1024));
	printf("%d\n", mul(-402, 4096));
	return (0);
}
